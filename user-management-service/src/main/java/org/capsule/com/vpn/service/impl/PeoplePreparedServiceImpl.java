package org.capsule.com.vpn.service.impl;

import org.capsule.com.vpn.controller.model.response.BasedUserInformationDto;
import org.capsule.com.vpn.service.PeoplePreparedService;
import org.springframework.stereotype.Service;

@Service
public class PeoplePreparedServiceImpl implements PeoplePreparedService {

    @Override
    public BasedUserInformationDto getBasedInformation(Long id) {
        return null;
    }
}
